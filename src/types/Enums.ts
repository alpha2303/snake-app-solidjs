export enum Direction {
    UP = 0,
    LEFT,
    DOWN,
    RIGHT
};

export enum GameState {
    OVER = 0,
    NEW,
    PAUSED,
    RUNNING
};

export enum CellColor {
    WHITE = 0,
    RED,
    GREEN,
};